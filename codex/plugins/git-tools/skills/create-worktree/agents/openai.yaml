interface:
  display_name: "Create Worktree"
  short_description: "Create a branch worktree in .worktree"
  default_prompt: "Use $create-worktree to create a new git worktree in the repo's .worktree directory for a branch name I provide."

policy:
  allow_implicit_invocation: true
